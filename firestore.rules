rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read and write to all collections for flawless real-time editing
    match /{document=**} {
      allow read, write: if true;
    }

  }
}
